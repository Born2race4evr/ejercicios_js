function recuentoImgs() {
  let totalImgs = document.getElementsByTagName("img").length;
  alert("Hay un total de " + totalImgs + " imagenes" );
}

function recuentoLinks() {
  let totalLinks = document.getElementsByTagName("a").length;
  alert("Hay un total de " + totalLinks + " links" );
}